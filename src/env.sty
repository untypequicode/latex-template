% env.sty --- Variables d'environnement du projet (copie locale de env.example.sty)
% -----------------------------------------------------------------------------
% Ce fichier centralise les valeurs que vous modifierez couramment pour le
% modèle LaTeX (logo, chemins, métadonnées du document, entrées de l'équipe).
%
% Flux de travail recommandé :
%  - Conserver `env.example.sty` dans le dépôt comme référence.
%  - Copier `env.example.sty` vers `src/env.sty` sur votre machine et modifier ce
%    fichier pour définir les valeurs spécifiques au projet (ou créer
%    `src/local_env.tex` avec des lignes \renewcommand pour écraser les valeurs
%    sans modifier ce fichier).
%
% Important : ce fichier déclare uniquement des variables via \providecommand
% afin que les remplacements avec \renewcommand soient sûrs et simples.
% Il NE charge PAS de paquets ni n'exécute d'effets de bord.
% -----------------------------------------------------------------------------
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{env}[2025/11/19 v1.0 Project environment variables]

% ---------------------------------------------------------------------------
% Configuration des images / ressources
% ---------------------------------------------------------------------------
% Une liste adaptée à l'utilisation avec \graphicspath (conserver la forme à
% double-accolade), par exemple :
%   {{img/}{images/}}
\providecommand{\envGraphicPaths}{{img/}{images/}{assets/img/}}

% Fichier du logo utilisé sur la page de titre (nom de fichier ou chemin relatif).
% Exemples :
%   \renewcommand{\envLogoFile}{img/university-logo.png}
%   \renewcommand{\envLogoFile}{images/logo.pdf}
\providecommand{\envLogoFile}{img/logo.png}

% Échelle appliquée au logo lorsqu'il est inclus via \includegraphics[scale=...]{...}
\providecommand{\envLogoScale}{0.20}

% ---------------------------------------------------------------------------
% Métadonnées du document (modifiez ces valeurs pour mettre à jour les en-têtes/page de titre/etc.)
% ---------------------------------------------------------------------------
% Faculté / institution
\providecommand{\envFaculty}{Nom de la faculté}

% Nom ou code du cours
\providecommand{\envCourse}{Nom du cours}

% Titre principal du document
\providecommand{\envTitle}{Titre du document}

% Auteur(s)
\providecommand{\envAuthor}{Auteur.e.s du document}

% Chaîne de date simple pour la remise / soumission
\providecommand{\envRemise}{Date de remise}

% Valeur formatée optionnelle pour la remise (par défaut \envRemise)
\providecommand{\envRemiseFmt}{\envRemise}

% Professeur(s) ou enseignant(s)
\providecommand{\envProfessor}{Professeur.e.s du cours}

% Nom de l'équipe ou description courte (utilisé sur la page de titre)
\providecommand{\envTeam}{Équipe de travail}

% ---------------------------------------------------------------------------
% Entrées de l'équipe / tableau des membres
% ---------------------------------------------------------------------------
% Fournissez le contenu qui sera inséré à l'intérieur d'un environnement tabular.
% Chaque ligne doit se terminer par \\ et respecter le format du tableau LaTeX.
%
% Exemple d'écrasement :
%   \renewcommand{\envTeamEntries}{%
%     123456 & Alice Exemple \\%
%     234567 & Bob Exemple   \\%
%   }
\providecommand{\envTeamEntries}{%
  matricule1 & nom1 \\%
  matricule2 & nom2 \\%
}

% ---------------------------------------------------------------------------
% Exemples rapides et guide (copier dans local_env.tex ou dans env.sty)
% ---------------------------------------------------------------------------
% Exemple : remplacer plusieurs valeurs (mettre ces lignes dans src/local_env.tex
% ou dans le préambule du document si vous préférez) :
%
% \renewcommand{\envTitle}{Mon rapport de projet}
% \renewcommand{\envAuthor}{Prénom Nom}
% \renewcommand{\envLogoFile}{img/my-logo.png}
% \renewcommand{\envLogoScale}{0.18}
% \renewcommand{\envFaculty}{Faculté de Génie}
% \renewcommand{\envCourse}{INF-1234: Algorithmique}
% \renewcommand{\envTeamEntries}{%
%   201234 & Alice Exemple \\%
%   201235 & Bob Exemple   \\%
% }
%
% Si vous utilisez \graphicspath dans votre préambule, vous pouvez appliquer :
%   \graphicspath\envGraphicPaths
% pour utiliser les chemins de recherche configurés.
%
% ---------------------------------------------------------------------------
% Alias de compatibilité ascendante
% ---------------------------------------------------------------------------
% Fournir des alias nommés comme dans le modèle d'origine afin que les fichiers
% qui utilisent encore les anciennes commandes (\cours, \titre, \auteur,
% \remise, \professeur, \equipe, etc.) continuent de fonctionner. Ces alias
% renvoient aux nouvelles variables \env...
%
% Remarque : ce sont de simples entrées \providecommand et elles peuvent être
% remplacées localement via \renewcommand si nécessaire.
\providecommand{\cours}{\envCourse}
\providecommand{\titre}{\envTitle}
\providecommand{\auteur}{\envAuthor}
\providecommand{\remise}{\envRemiseFmt}
\providecommand{\professeur}{\envProfessor}
\providecommand{\equipe}{\envTeam}

% Alias pour les macros d'affichage utilisées précédemment. Elles fournissent les anciens noms de macro
% mais dépendent de paquets chargés au niveau du document (graphicx, float) dans
% le préambule où elles sont effectivement utilisées.
\providecommand{\projectLogo}{\IfFileExists{\envLogoFile}{\includegraphics[scale=\envLogoScale]{\envLogoFile}}{\fbox{\begin{minipage}[c][3cm][c]{0.65\textwidth}\centering\textbf{Logo manquant}\\[4pt]\small\texttt{\envLogoFile}\end{minipage}}}}
\providecommand{\printTeamTable}{\begin{table}[H]\centering\begin{tabular}{|c|c|}\hline\textbf{Matricule} & \textbf{Nom} \\\hline \envTeamEntries \hline\end{tabular}\caption{Matricules et noms des membres de l'équipe}\end{table}}

% ---------------------------------------------------------------------------
% Fin de env.sty
% -----------------------------------------------------------------------------
\endinput
